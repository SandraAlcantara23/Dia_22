#include <stdio.h>
int main(){
	int an;
	int seg;

	printf("Cuantos aros tienes:\n");
	scanf("%d", &an);
	seg = an * 365 * 24 * 60 * 60;
	
printf("La cantidad de segundos que has vivido es: %d\n", seg);
	s
	return 0;
}

